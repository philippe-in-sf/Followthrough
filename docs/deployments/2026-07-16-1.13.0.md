# Deployment Audit - 2026-07-16 - 1.13.0

- Previous version: 1.12.0
- New version: 1.13.0
- Version increment: minor release
- Deployment target: production
- Audit note: this release records the deployment version bump for task status closure handling and long-link rendering regression fixes.

## Release Contents

- Add `Won't Fix` as a task status across validation, filters, task editing, and task lanes.
- Treat `Done` and `Won't Fix` as closed statuses for task alerts, reminders, dependency warnings, dashboard open-work queries, and recurring-meeting task carry-over.
- Add a SQLite migration to preserve existing tasks while expanding the task status constraint.
- Fix leaked Google Slides fragments after collapsed markdown links in task titles and notes.
- Add regression coverage for closed task behavior and split long-link fragments.

## Verification Notes

- `npm run test -- tests/server/tasks.test.ts tests/client/rich-notes.test.tsx tests/client/linked-text.test.tsx tests/client/app-shell.test.tsx` completed successfully.
- `npm run build` completed successfully.
- `git diff --check` completed successfully.
