# Deployment 2026-07-17 - 1.15.0

## Scope

- Added dashboard momentum metrics for completed tasks, decisions, and meetings.
- Added Markdown workspace summary export from the dashboard.
- Added per-user opt-in weekly workspace digest emails with default-off preferences and weekly send throttling.

## Verification

- `npm run check`
- `npm test -- --run`

## Deployment Notes

- Version bumped from `1.14.0` to `1.15.0` before deployment.
- New database migration: `025_weekly_digest_preferences.sql`.
