# Deployment Audit - 2026-07-15 - 1.10.0

- Previous version: 1.9.1
- New version: 1.10.0
- Version increment: minor release
- Deployment target: production
- Audit note: this release records the deployment version bump for admin member impersonation.

## Release Contents

- Add a session-backed admin ability to view the app as a visible member user.
- Preserve existing admin team scoping, with owner-level access retaining cross-team visibility.
- Show a persistent impersonation banner with a clear stop action.
- Hide admin navigation while viewing as a member.
- Block write actions while impersonating so review sessions stay read-only.

## Verification Notes

- `git diff --check` completed successfully.
- A direct SQLite migration sanity check confirmed the new `sessions.impersonated_user_id` column and index.
- The Vite client build portion of `npm run build` completed successfully.
- The local TypeScript/Vitest runner and server compile step hung without diagnostics in this environment before deployment packaging.
