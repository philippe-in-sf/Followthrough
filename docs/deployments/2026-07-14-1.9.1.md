# Deployment Audit - 2026-07-14 - 1.9.1

- Previous version: 1.9.0
- New version: 1.9.1
- Version increment: patch release
- Deployment target: production
- Audit note: this release records the deployment version bump for rich note link recovery on task cards.

## Release Contents

- Recover malformed Google Slides markdown fragments that were stored outside the auto-created link wrapper.
- Keep task-card notes displaying compact `Link` text instead of leaked `00#slide...00` fragments.
- Normalize selected URL text before the rich notes editor wraps it as a markdown link.

## Verification Notes

- `npm run test -- tests/client/linked-text.test.tsx tests/client/rich-notes.test.tsx tests/client/dashboard.test.tsx` completed successfully.
- `npm run check` completed successfully.
- `npm run build` completed successfully.
- `git diff --check` completed successfully.
