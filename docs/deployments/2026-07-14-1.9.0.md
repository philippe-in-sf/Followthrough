# Deployment Audit - 2026-07-14 - 1.9.0

- Previous version: 1.8.0
- New version: 1.9.0
- Version increment: minor release
- Deployment target: production
- Audit note: this release records the deployment version bump for expandable dashboard assignee task lists and reusable pagination across long rendered app lists.

## Release Contents

- Make dashboard "Open tasks by assignee" rows expandable.
- Show each assignee's open tasks inline from the dashboard.
- Add reusable pagination controls for long rendered lists.
- Apply pagination to dashboard lists, task lanes, meeting lanes, decisions, people, meeting notes, admin users, login logs, waitlist signups, audit history, related records, dependency lists, checkbox pickers, and calendar import results.

## Verification Notes

- `npx tsc --noEmit -p tsconfig.json --pretty false` completed successfully.
- `npx tsc --noEmit -p tsconfig.server.json --pretty false` completed successfully.
- `npx tsc --noEmit -p tsconfig.test.json --pretty false` completed successfully.
- `git diff --check` completed successfully.
- `npm run build` completed successfully.
