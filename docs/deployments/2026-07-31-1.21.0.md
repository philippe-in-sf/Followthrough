# Deployment Audit - 2026-07-31 - 1.21.0

- Previous version: 1.20.0
- New version: 1.21.0
- Version increment: minor release
- Deployment target: production
- Audit note: this release refreshes compatible application and development dependencies and adopts the validated TypeScript 7 compiler.

## Release Contents

- Update React, React DOM, express-rate-limit, and lucide-react within their existing compatibility ranges.
- Update Vite, its React plugin, Vitest, tsx, and matching Node, React, React DOM, and Supertest types.
- Update jest-dom from 6.9.1 to 7.0.0.
- Update TypeScript from 6.0.3 to 7.0.2 and retain Node 24 type definitions.
- Keep jsdom on 29.1.1 until Followthrough's minimum Node version is raised to jsdom 30's requirement.
- Use `import.meta.dirname` in the Vite alias configuration for native config-loader compatibility.

## Verification Notes

- `npm ci` completed successfully from an isolated non-File-Provider workspace.
- `npm run check` completed successfully with TypeScript 7.
- `npm test` completed successfully with 353 passing tests across 46 test files.
- `npm run build` completed successfully with Vite 8.2.0 and no config-loader warning.
- `npm audit --audit-level=high` reported zero vulnerabilities.
- `git diff --check` completed successfully.
- GitHub CI remains the authoritative release gate.
- Production verification will use `/api/health` and `/api/version` after deployment.
