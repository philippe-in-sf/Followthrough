# Deployment Audit - 2026-07-15 - 1.10.1

- Previous version: 1.10.0
- New version: 1.10.1
- Version increment: patch release
- Deployment target: production
- Audit note: this release adds authentication and request-boundary defense in depth.

## Release Contents

- Limit failed login attempts independently by client IP and normalized email address without changing the invalid-credential response.
- Apply separate limits to signup and password-reset requests.
- Reject cross-origin browser state changes and allow CORS only for the configured public application origin.
- Add Helmet baseline response headers and trust forwarded client addresses only from the loopback production proxy.
- Clear the reported `undici` and `form-data` dependency advisories.

## Verification Notes

- `npm audit fix` completed with zero remaining vulnerabilities.
- `npm run check` completed successfully.
- All 243 tests passed.
- `npm run build` completed successfully.
- `git diff --check` completed successfully.
