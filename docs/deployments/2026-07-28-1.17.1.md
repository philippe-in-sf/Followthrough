# Deployment Audit - 2026-07-28 - 1.17.1

- Previous version: 1.17.0
- New version: 1.17.1
- Version increment: patch release
- Deployment target: production
- Audit note: this release fixes guided-tour positioning and context wording on mobile screens.

## Release Contents

- Position the guided-tour card above or below the active mobile target instead of forcing it above the bottom navigation.
- Highlight the visible mobile section summary when the desktop context rail is hidden.
- Replace ambiguous references to “this rail” with descriptions of the highlighted controls.
- Add regression coverage for mobile tour geometry and copy.

## Verification Notes

- `npm run check` completed successfully.
- `npm test` completed successfully with 347 passing tests.
- `npm run build` completed successfully.
- `git diff --check` completed successfully.
- Production verification will use `/api/health` and `/api/version` after deployment.
