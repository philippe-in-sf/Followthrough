# Deployment Audit - 2026-07-18 - 1.16.0

- Previous version: 1.15.0
- New version: 1.16.0
- Version increment: minor release
- Deployment target: production
- Audit note: this release records the deployment version bump for the dark-mode task filter row regression.

## Release Contents

- Theme the task filter panel, toggle, chips, and expanded-field divider with skin variables.
- Keep the existing light-mode defaults intact while preventing the filter row from rendering white in dark skins.

## Verification Notes

- `npm run check` completed successfully before the release bump.
- `npm run build` completed successfully before the release bump.
- `git diff --check` completed successfully after the release bump.
- Post-bump `npm run check` and `npm run build` were interrupted after the local TypeScript steps stopped returning output in this managed environment.
