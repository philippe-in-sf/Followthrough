# Deployment Audit - 2026-07-28 - 1.18.0

- Previous version: 1.17.1
- New version: 1.18.0
- Version increment: minor release
- Deployment target: production
- Audit note: this release moves calendar configuration into Settings and closes the mobile installability gap found during feature-parity review.

## Release Contents

- Move Google Calendar connection controls, private iCalendar feed configuration, and the calendar shortcut from Meetings to Settings.
- Keep meeting import workflows on the Meetings page while sourcing their connection state from the shared app state.
- Update Settings navigation context so mobile users can discover calendar and digest controls.
- Add web app manifest metadata and dedicated 192px and 512px icons for home-screen installation.
- Add regression coverage for calendar shortcut, Google Calendar disconnect, and private iCalendar feed settings.

## Verification Notes

- TypeScript validation completed successfully for the client, server, and test configurations.
- `npm run build` completed successfully.
- `git diff --check` completed successfully.
- Focused Vitest runs were attempted locally but stalled before reporting results; GitHub CI is the authoritative test gate for this release.
- Production verification will use `/api/health` and `/api/version` after deployment.
